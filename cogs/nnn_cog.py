from discord.ext import commands
import datetime
from random import randint

class command(commands.Cog):
    @commands.hybrid_command()
    async def sex(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def swordfight(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def impregnate(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def threesome(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def sniff(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def cbt(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def stats(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))

    @commands.hybrid_command()
    async def leaderboard(self, ctx, *, user: str):
        chance = randint(1,100)
        if chance == 69:
            await ctx.send(f"{ctx.author.mention} has been apprehended by the Special Forces of Sex and was killed.")
            await ctx.author.timeout(datetime.timedelta(minutes=5))
        else:
            await ctx.send(f"{ctx.author.mention} has violated the sex curfew.")
            await ctx.author.timeout(datetime.timedelta(seconds=60))